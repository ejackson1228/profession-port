{
  "name": "profession-port",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "build": "cd client && npm run build",
    "predeploy": "cd client && npm run build",
    "deploy": "cd client && gh-pages -d build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ejackson1228/profession-port.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ejackson1228/profession-port/issues"
  },
  "homepage": "https://ejackson1228.github.io/profession-port",
  "dependencies": {
    "concurrently": "^7.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^6.8.2",
    "nodemailer": "^6.8.0"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  }
}
